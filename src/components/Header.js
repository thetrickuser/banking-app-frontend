import React from "react";
import styles from "./Header.module.css";

const Header = () => {
  return (
    <div className={styles.header}>
      <p className="header-title">Free Movie Tickets</p>
    </div>
  );
};

export default Header;
